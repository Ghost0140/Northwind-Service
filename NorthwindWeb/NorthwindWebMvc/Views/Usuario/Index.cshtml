@model IEnumerable<NorthwindWebMvc.Models.Usuario>

@{
    ViewData["Title"] = "Usuarios";
}

<div class="crud-page">
    <div class="container">
        <div class="card crud-card">

            <!-- HEADER -->
            <div class="card-header d-flex justify-content-between align-items-center">
                <h4 class="mb-0">
                    <i class="bi bi-people"></i> Listado de Usuarios
                </h4>

                <div class="d-flex gap-2">
                    <a asp-controller="Home" asp-action="Index"
                       class="btn btn-outline-light btn-sm btn-action">
                        <i class="bi bi-house"></i> Inicio
                    </a>

                    <a asp-action="Create"
                       class="btn btn-outline-light btn-sm btn-action">
                        <i class="bi bi-plus-circle"></i> Nuevo
                    </a>
                </div>
            </div>

            <!-- MENSAJES -->
            @if (TempData["mensaje"] != null)
            {
                <div class="alert alert-info alert-dismissible fade show m-3">
                    <i class="bi bi-info-circle-fill"></i>
                    @TempData["mensaje"]
                    <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                </div>
            }

            <!-- BODY -->
            <div class="card-body bg-white">

                <div class="table-responsive">
                    <table class="table table-hover align-middle text-center crud-table">
                        <thead>
                            <tr>
                                <th>ID</th>
                                <th>Usuario</th>
                                <th>Clave</th>
                                <th>Rol</th>
                                <th>Estado</th>
                                <th>Acciones</th>
                            </tr>
                        </thead>
                        <tbody>

                            @if (!Model.Any())
                            {
                                <tr>
                                    <td colspan="6" class="text-muted py-3">
                                        No hay usuarios registrados.
                                    </td>
                                </tr>
                            }
                            else
                            {
                                foreach (var item in Model)
                                {
                                    <tr>
                                        <td>@item.IdUsuario</td>
                                        <td>@item.NombreUsuario</td>
                                        <td>@item.Clave</td>
                                        <td>@item.Rol</td>
                                        <td>
                                            <span class="badge @(item.Estado ? "bg-success" : "bg-secondary")">
                                                @(item.Estado ? "Activo" : "Inactivo")
                                            </span>
                                        </td>
                                        <td>
                                            @if (Context.Session.GetString("rol") == "Admin")
                                            {
                                                <a asp-action="Edit" asp-route-id="@item.IdUsuario"
                                                   class="btn btn-sm btn-outline-warning me-2">
                                                    <i class="bi bi-pencil"></i>
                                                </a>

                                                <a asp-action="Delete" asp-route-id="@item.IdUsuario"
                                                   class="btn btn-sm btn-outline-danger"
                                                   onclick="return confirm('¿Eliminar usuario?');">
                                                    <i class="bi bi-trash"></i>
                                                </a>
                                            }
                                        </td>
                                    </tr>
                                }
                            }

                        </tbody>
                    </table>
                </div>

            </div>
        </div>
    </div>
</div>